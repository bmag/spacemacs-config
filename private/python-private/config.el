;; anaconda: goto at-pt, help at-pt, refs at-pt
;; imenu: goto at-file
;; cscope: goto at-proj, refs at-proj (lesser than anaconda's refs at-pt)
;; pylookup: help stdlib, help 3rd-party (with configuration)?
;; pydoc: help stdlib, help 3rd-party, search by module name only (configurable?)

;; integrations:
;; anaconda + imenu: help at-file, refs at-file
;; anaconda + cscope: help at-proj, refs at-proj (better than cscope alone?)

;; missing:
;; goto 3rd-party, refs 3rd-party (given by cscope?)
;; goto stdlib, refs stdlib (given by cscope?)
